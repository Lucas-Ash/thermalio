# Optional / test-time dependencies for Thermalio's V&V tooling.
# The core solvers (heat_solver, excluding heat_solver.mms) need only
# numpy / scipy / matplotlib. SymPy powers the manufactured-solution
# auto-derivation in heat_solver/mms.py and is imported lazily, so it is
# required only to build/verify MMS sources, not to run the solvers.
pytest
sympy
# Optional PINN backend; install manually when running tests/test_pinn_jax.py
# against the real backend:
# jax
# jaxlib
